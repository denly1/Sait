# 🚀 Инструкция по установке проекта EMPTY GAZ

## Для друга-разработчика

Привет! Эта инструкция поможет тебе установить проект и начать работать над ним.

---

## 📋 Что нужно установить

### 1. Git
Скачай и установи Git:
- **Windows**: https://git-scm.com/download/win
- **Mac**: `brew install git`
- **Linux**: `sudo apt-get install git`

### 2. Node.js
Скачай и установи Node.js (версия 18 или выше):
- https://nodejs.org/
- Выбери LTS версию (рекомендуется)

### 3. Редактор кода
Рекомендую **VS Code**:
- https://code.visualstudio.com/

---

## 🔧 Установка проекта

### Шаг 1: Клонируй репозиторий

Открой терминал (командную строку) и выполни:

```bash
git clone https://github.com/denly1/Sait.git
cd Sait/project
```

### Шаг 2: Переключись на свою ветку

```bash
git checkout dev-friend
```

Это твоя личная ветка для разработки! Работай только в ней.

### Шаг 3: Установи зависимости

```bash
npm install
```

Это установит все необходимые библиотеки.

### Шаг 4: Запусти проект

```bash
npm run dev
```

Проект откроется в браузере по адресу: `http://localhost:5173`

---

## 💻 Как работать с Git

### Перед началом работы (каждый раз!)

```bash
# Убедись что ты на своей ветке
git branch

# Должно показать: * dev-friend

# Получи последние изменения
git pull origin dev-friend
```

### Сохранение своих изменений

```bash
# 1. Посмотри что изменилось
git status

# 2. Добавь все изменения
git add .

# 3. Сделай коммит с описанием
git commit -m "Описание того, что ты сделал"

# 4. Отправь на GitHub
git push origin dev-friend
```

### Примеры хороших коммитов:

```bash
git commit -m "Добавил новую кнопку в header"
git commit -m "Исправил баг с корзиной"
git commit -m "Улучшил дизайн карточек товаров"
```

---

## 🔄 Синхронизация с основной веткой

Если нужно получить изменения из главной ветки (main):

```bash
# Переключись на main
git checkout main

# Получи последние изменения
git pull origin main

# Вернись на свою ветку
git checkout dev-friend

# Слей изменения из main в свою ветку
git merge main

# Если будут конфликты - напиши мне!
```

---

## 📁 Структура проекта

```
project/
├── src/
│   ├── components/     # Компоненты React
│   │   ├── Header.tsx
│   │   ├── EscortSection.tsx
│   │   ├── ProductsSection.tsx
│   │   └── ...
│   ├── lib/           # Утилиты и хранилище
│   │   └── localStorage.ts
│   ├── utils/         # Вспомогательные функции
│   │   └── analytics.ts
│   ├── App.tsx        # Главный компонент
│   └── main.tsx       # Точка входа
├── public/            # Статические файлы
└── package.json       # Зависимости проекта
```

---

## 🎨 Технологии в проекте

- **React 18** - библиотека для UI
- **TypeScript** - типизированный JavaScript
- **Vite** - сборщик проекта (очень быстрый!)
- **TailwindCSS** - стили
- **Lucide React** - иконки

---

## 🐛 Частые проблемы

### Ошибка: "npm: command not found"
- Переустанови Node.js
- Перезапусти терминал

### Ошибка: "Permission denied"
**Windows**: Запусти терминал от имени администратора
**Mac/Linux**: Используй `sudo` перед командой

### Проект не запускается
```bash
# Удали node_modules и установи заново
rm -rf node_modules
npm install
npm run dev
```

### Git конфликты
- Напиши мне в телеграм/дискорд
- Я помогу разрешить конфликты

---

## 📝 Правила работы

### ✅ МОЖНО:
- Работать в ветке `dev-friend`
- Создавать новые компоненты
- Улучшать существующий код
- Экспериментировать
- Делать коммиты часто

### ❌ НЕЛЬЗЯ:
- Работать в ветке `main` напрямую
- Пушить без коммита
- Удалять чужие файлы без согласования
- Коммитить с сообщением "fix" или "update"

---

## 🎯 Задачи для начала

1. **Запусти проект** - убедись что все работает
2. **Изучи структуру** - посмотри файлы в `src/components`
3. **Сделай тестовое изменение**:
   - Открой `src/App.tsx`
   - Измени какой-нибудь текст
   - Сохрани и посмотри в браузере
4. **Сделай первый коммит**:
   ```bash
   git add .
   git commit -m "Мой первый коммит - тестовое изменение"
   git push origin dev-friend
   ```

---

## 📞 Связь

Если что-то не работает или есть вопросы:
- Пиши мне в телеграм/дискорд
- Создай Issue на GitHub
- Звони/пиши

---

## 🚀 Полезные команды

```bash
# Посмотреть статус
git status

# Посмотреть историю коммитов
git log --oneline

# Отменить последний коммит (если еще не запушил)
git reset HEAD~1

# Посмотреть изменения в файле
git diff имя_файла

# Создать новую ветку от своей
git checkout -b feature/новая-фича

# Вернуться к последней версии файла
git checkout -- имя_файла
```

---

## 📚 Полезные ресурсы

- **React документация**: https://react.dev/
- **TypeScript**: https://www.typescriptlang.org/docs/
- **TailwindCSS**: https://tailwindcss.com/docs
- **Git шпаргалка**: https://education.github.com/git-cheat-sheet-education.pdf

---

## ✨ Советы

1. **Делай коммиты часто** - лучше 10 маленьких, чем 1 огромный
2. **Пиши понятные сообщения** - через месяц сам не вспомнишь что делал
3. **Тестируй перед пушем** - запусти проект и проверь что ничего не сломалось
4. **Не бойся экспериментировать** - всегда можно откатить изменения
5. **Спрашивай если непонятно** - лучше спросить, чем сломать

---

**Удачи в разработке! 🎉**

P.S. Если что-то пошло не так - не паникуй, всегда можно откатить изменения через Git!

# 🌿 Информация о ветках проекта

## Структура веток

### 🔴 `main` - Главная ветка (ТЫ)
- Это твоя основная ветка
- Здесь стабильная версия проекта
- Работай здесь как обычно

### 🟢 `dev-friend` - Ветка друга
- Создана специально для твоего друга
- Он будет работать только в этой ветке
- Его изменения не повлияют на твою работу

---

## 📝 Для тебя (владелец проекта)

### Твоя обычная работа:

```bash
# Работай как обычно в main
git add .
git commit -m "Твои изменения"
git push origin main
```

### Если хочешь посмотреть что делает друг:

```bash
# Переключись на его ветку
git checkout dev-friend

# Посмотри изменения
git log

# Вернись обратно
git checkout main
```

### Если хочешь взять изменения друга к себе:

```bash
# Убедись что ты на main
git checkout main

# Слей его ветку в свою
git merge dev-friend

# Запуши изменения
git push origin main
```

---

## 📝 Что отправить другу

Отправь ему файл **SETUP_FOR_FRIEND.md** и скажи:

1. **Склонировать репозиторий:**
   ```bash
   git clone https://github.com/denly1/Sait.git
   cd Sait/project
   ```

2. **Переключиться на свою ветку:**
   ```bash
   git checkout dev-friend
   ```

3. **Установить зависимости:**
   ```bash
   npm install
   ```

4. **Запустить проект:**
   ```bash
   npm run dev
   ```

5. **Работать и пушить:**
   ```bash
   git add .
   git commit -m "Описание изменений"
   git push origin dev-friend
   ```

---

## 🔄 Workflow (как это работает)

```
main (ты)          dev-friend (друг)
   |                      |
   |---изменения--->      |
   |                      |---изменения--->
   |                      |
   |<---merge (если нужно)|
   |                      |
```

### Преимущества:
- ✅ Вы не мешаете друг другу
- ✅ Каждый работает в своей ветке
- ✅ Можно легко смержить изменения
- ✅ Если что-то сломается - не влияет на main

---

## 🚨 Важно!

### Друг должен ВСЕГДА:
1. Работать только в ветке `dev-friend`
2. Делать `git pull origin dev-friend` перед работой
3. Пушить в `dev-friend`, НЕ в `main`

### Ты можешь:
1. Работать в `main` как обычно
2. Смотреть что делает друг в его ветке
3. Мержить его изменения когда захочешь

---

## 🎯 Быстрые команды

### Посмотреть все ветки:
```bash
git branch -a
```

### Посмотреть на какой ветке ты сейчас:
```bash
git branch
```

### Удалить ветку друга (если нужно):
```bash
git branch -d dev-friend
git push origin --delete dev-friend
```

### Создать еще одну ветку для другого друга:
```bash
git checkout -b dev-friend2
git push -u origin dev-friend2
```

---

## 📞 Если что-то пошло не так

### Друг случайно запушил в main:
```bash
# Откати последний коммит в main
git checkout main
git reset --hard HEAD~1
git push origin main --force
```

### Конфликты при мерже:
1. Открой файлы с конфликтами
2. Найди строки с `<<<<<<<` и `>>>>>>>`
3. Выбери нужный вариант
4. Удали маркеры конфликта
5. Сделай коммит

---

**Все готово! Друг может начинать работать! 🎉**
